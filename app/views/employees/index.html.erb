<h1>All employees</h1>
<% @employees.each do |employee| %>
  <hr>
  <h3><%= employee["name"] %></h3>
  <p><%= employee["job_titles"] %></p>
  <p>Salary: <%= number_to_currency(employee["employee_annual_salary"]) %></p>
<% end %>